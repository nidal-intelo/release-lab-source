[project]
name = "solver"
version = "0.0.0"  # inert -- nothing resolves by this; the checked-out source is what ships
description = "Stand-in for the solver package"
requires-python = ">=3.9"

[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"
