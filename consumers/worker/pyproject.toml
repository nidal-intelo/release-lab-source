[project]
name = "worker"
version = "0.0.0"  # inert -- nothing resolves by this
description = "Stand-in for the replenishment worker"
requires-python = ">=3.9"
dependencies = [
    "qb",
]

[tool.uv.sources]
qb = { workspace = true }

[build-system]
requires = ["uv_build>=0.11,<0.13"]
build-backend = "uv_build"
